# Screenshots

Drop dashboard screenshots here and reference them from the top-level README:

- `dashboard.png`  - the Jobs tab (ranked list, stat strip, columns)
- `preview.png`    - the slide-in preview pane for a single posting
- `settings.png`   - the Settings tab (search terms + source toggles)

Keep them free of real personal data (blur or use example rows if needed).
