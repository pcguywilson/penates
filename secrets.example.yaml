# Copy to secrets.yaml (git-ignored) and fill in your Workday login, if you use
# the WIP Workday runner. One login is reused across Workday tenants. Local only.
workday:
  email: you@example.com
  password: "CHANGE-ME"
