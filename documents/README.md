Drop your resume PDFs in `resumes/` and cover letters in `covers/`.
The tool auto-picks the newest by a date in the filename (e.g. `Resume_2026-09.pdf`),
then by modified time. PDFs are git-ignored and never committed.
